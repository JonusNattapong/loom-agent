# Release notes

Add a changeset to every user-facing change:

```text
npx changeset
```

Choose the affected packages and a semver bump. The release workflow opens a
version/changelog pull request; merging it publishes all public workspaces.
