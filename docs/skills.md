# Skills

Project skills live under `.loom/skills/<name>/SKILL.md`. Each file may contain YAML-like frontmatter with `name`, `description`, and a comma-separated `tools` list, followed by instructions. Loom discovers skills deterministically and only injects explicitly selected skills, for example `loom run "review this" --skill code-review`.
