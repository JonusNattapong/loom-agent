{
  "name": "@loom/bots",
  "description": "Bot runtime and event gateway",
  "private": true,
  "version": "0.5.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@loom/coordinator": "workspace:*",
    "@loom/core": "workspace:*",
    "@loom/state": "workspace:*",
    "discord.js": "^14.0.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/discord.js": "^0.0.0"
  }
}