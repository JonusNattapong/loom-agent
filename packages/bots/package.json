{
  "name": "@loom/bots",
  "description": "Bot runtime and event gateway",
  "private": true,
  "version": "0.5.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "discord.js": "^14.27.0"
  },
  "devDependencies": {}
}
