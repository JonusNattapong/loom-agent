import {describe,it,expect} from "vitest";
import {ToolExecutor,ToolRegistry} from "./index.js";
describe("ToolExecutor middleware",()=>{it("denies, limits, and reuses successful idempotent calls",async()=>{const r=new ToolRegistry().register({name:"safe",description:"safe",execute:async()=>"123456"});const ledger=new Map<string,any>();const policy={maxResultChars:3,ledger:{getToolExecution:(id:string)=>ledger.get(id),recordToolExecution:(id:string,_a:string,_n:string,_i:Record<string,unknown>,status:string,result?:string)=>ledger.set(id,{status,result})}};const e=new ToolExecutor(r,policy);await expect(e.execute("safe",{}, {agentId:"a",toolCallId:"c"})).resolves.toBe("123");await expect(e.execute("safe",{}, {agentId:"a",toolCallId:"c"})).resolves.toBe("123");await expect(new ToolExecutor(r,{permissions:{safe:"deny"}}).execute("safe",{})).rejects.toThrow("denied");});});
