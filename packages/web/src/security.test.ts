import {describe,expect,it} from "vitest";import {readFileSync} from "node:fs";import {fileURLToPath} from "node:url";import {text} from "./types";import {routeHref} from "./routing";
describe("web trust boundary",()=>{it("keeps hostile runtime text inert as data",()=>{const hostile="<script>alert(1)</script><img src=x onerror=alert(1)>";expect(text(hostile)).toBe(hostile);expect(routeHref("agents",hostile)).not.toContain("<script>");});it("does not use raw HTML injection or unsafe URL schemes",()=>{const files=["app.tsx","pages.tsx","components.tsx","api.ts","hooks.ts"].map(file=>readFileSync(fileURLToPath(new URL(`./${file}`,import.meta.url)),"utf8")).join("\n");expect(files).not.toMatch(/dangerouslySetInnerHTML|innerHTML/);expect(files).not.toMatch(/javascript:/i);});});
